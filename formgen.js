
function readJSONTemplate(file) {
	//Reads data structure in JSON template
}

function fieldToHTML(field) {
	//Reads field parameters and returns html
}

function fieldJSValidation(field) {
	//Reads field parameters and returns javascript validation
}

function appendHTML(html) {
	//Appends field html to form
}

function appendJS(js) {
	//Appends validation script to js
}

function generateForm(file) {
	//returns HTML form
}	

function generateApp(file) {
	//returns zipped node application containing:
	//node server file
	//html form
	//javascript form validation
}